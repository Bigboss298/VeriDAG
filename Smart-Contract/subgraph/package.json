{
  "name": "datatrust-nexus-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for DataTrust Nexus",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create --node http://localhost:8020/ datatrust-nexus",
    "remove-local": "graph remove --node http://localhost:8020/ datatrust-nexus",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 datatrust-nexus",
    "deploy": "graph deploy --studio datatrust-nexus",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.56.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.6.0"
  }
}

